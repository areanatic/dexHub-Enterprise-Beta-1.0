# DexHub Personal Profile Schema v1.2
# Single Source of Truth for profile.yaml structure

schema_version: "1.2"
created: "2025-11-16"
updated: "2026-04-20"
status: "active"
# v1.1 (2026-04-19): added company.* block (Q43-Q49 enterprise compliance fields)
#                    for onboarding v5.0. Backward-compatible — all new fields
#                    are optional; v1.0 profiles remain valid.
# v1.2 (2026-04-20): added `consents` array for saved-consent tracking (Phase
#                    5.1.c Tier 3.1). Additive — v1.0/v1.1 profiles remain valid.
#                    Feature: meta.saved_consent_tracking in features.yaml.

# CONTRACT DEFINITION
contract:
  producer:
    component: "myDex Onboarding"
    file: ".dexCore/core/agents/mydex-agent.md"
    output_file: "myDex/.dex/config/profile.yaml"
    responsibility: "Generate valid profile.yaml from onboarding questions"
    validation: "MUST validate output against this schema before saving"

  consumer:
    component: "DexHub Workflow Engine"
    file: ".dexCore/core/tasks/workflow.xml"
    reads_from: "myDex/.dex/config/profile.yaml"
    consumes: "Session variables {profile_*}"
    responsibility: "Load profile and set session variables"
    validation: "MUST validate input against this schema on load"

  integration_test:
    file: "tests/integration/test_profile_integration.sh"
    validates: "End-to-end: Onboarding → Profile → Workflow → Agent"

# SCHEMA DEFINITION
fields:

  # META SECTION
  version:
    type: string
    required: true
    description: "Schema version (e.g., '1.0')"
    example: "1.0"
    producer_source: "Hardcoded by onboarding agent"
    consumer_usage: "Version compatibility check"

  created_at:
    type: string
    format: ISO8601
    required: true
    description: "Timestamp when profile was created"
    example: "2025-11-16T12:00:00Z"
    producer_source: "Generated by onboarding agent"
    consumer_usage: "Profile freshness tracking"

  last_updated:
    type: string
    format: ISO8601
    required: false
    description: "Timestamp of last profile update"
    example: "2025-11-16T14:30:00Z"
    producer_source: "Updated on profile modification"
    consumer_usage: "Change tracking"

  # PERSONALIZATION SECTION
  personalization:
    type: object
    required: true
    description: "User personalization preferences"

    fields:
      name:
        type: string
        required: true
        description: "User's preferred name (first name or nickname)"
        example: "Alex"
        producer_source: "Q1 in onboarding-questions.yaml"
        consumer_usage: "Personalized greetings in agents"
        validation:
          min_length: 1
          max_length: 100

      language:
        type: enum
        required: true
        values: ["de", "en", "es", "fr", "it", "pt"]
        default: "en"
        description: "Preferred communication language"
        example: "de"
        producer_source: "Q2 in onboarding-questions.yaml"
        consumer_usage: "{profile_language} in workflow.xml, agent responses"
        validation:
          allowed_values: ["de", "en", "es", "fr", "it", "pt"]

      role:
        type: enum
        required: true
        values: ["developer", "architect", "product_manager", "designer", "analyst", "student", "other"]
        default: "developer"
        description: "Primary professional role"
        example: "developer"
        producer_source: "Q3 in onboarding-questions.yaml"
        consumer_usage: "{profile_role} for role-specific content"
        validation:
          allowed_values: ["developer", "architect", "product_manager", "designer", "analyst", "student", "other"]

  # TECHNICAL SECTION
  technical:
    type: object
    required: true
    description: "Technical preferences and expertise"

    fields:
      experience_level:
        type: enum
        required: true
        values: ["junior", "intermediate", "senior", "expert"]
        default: "intermediate"
        description: "Overall technical experience level"
        example: "senior"
        producer_source: "Derived from experience_years (Q4) in onboarding"
        consumer_usage: "{profile_experience} for content complexity adaptation"
        mapping:
          "0-2 years": "junior"
          "3-5 years": "intermediate"
          "6-10 years": "senior"
          "10+ years": "expert"

      code_style:
        type: enum
        required: true
        values: ["airbnb", "google", "standard", "custom"]
        default: "standard"
        description: "Preferred code style guide"
        example: "airbnb"
        producer_source: "Q38 in onboarding-questions.yaml (NEW)"
        consumer_usage: "{profile_code_style} for code examples in agents"
        validation:
          allowed_values: ["airbnb", "google", "standard", "custom"]

      verbosity:
        type: enum
        required: true
        values: ["concise", "balanced", "detailed"]
        default: "balanced"
        description: "Preferred response verbosity level"
        example: "concise"
        producer_source: "Q39 in onboarding-questions.yaml (NEW)"
        consumer_usage: "{profile_verbosity} for response length adaptation"
        validation:
          allowed_values: ["concise", "balanced", "detailed"]

  # TECH STACK SECTION
  tech:
    type: object
    required: true
    description: "Technology stack preferences"

    fields:
      primary_stack:
        type: array
        item_type: string
        required: true
        min_items: 1
        max_items: 10
        description: "Primary technologies user works with"
        example: ["typescript", "react", "nodejs"]
        producer_source: "Q10-Q15 in onboarding-questions.yaml"
        consumer_usage: "{profile_tech_stack} for prioritized recommendations"
        validation:
          min_items: 1
          max_items: 10

      secondary_stack:
        type: array
        item_type: string
        required: false
        max_items: 10
        description: "Secondary/learning technologies"
        example: ["python", "docker"]
        producer_source: "Q16 in onboarding-questions.yaml (optional)"
        consumer_usage: "Additional context for recommendations"
        default: []

      interests:
        type: array
        item_type: string
        required: false
        max_items: 10
        description: "Technologies user wants to learn"
        example: ["rust", "kubernetes"]
        producer_source: "Q17 in onboarding-questions.yaml (optional)"
        consumer_usage: "Learning path suggestions"
        default: []

  # AI PREFERENCES SECTION
  ai:
    type: object
    required: true
    description: "AI tool preferences and readiness"

    fields:
      readiness:
        type: enum
        required: true
        values: ["beginner", "intermediate", "advanced", "expert"]
        default: "intermediate"
        description: "AI tool usage experience level"
        example: "advanced"
        producer_source: "Q20 in onboarding-questions.yaml"
        consumer_usage: "{profile_ai_readiness} for AI feature complexity"
        validation:
          allowed_values: ["beginner", "intermediate", "advanced", "expert"]

      copilot_enabled:
        type: boolean
        required: true
        default: false
        description: "GitHub Copilot usage status"
        example: true
        producer_source: "Q21 in onboarding-questions.yaml"
        consumer_usage: "Copilot-specific features and tips"

      preferred_model:
        type: enum
        required: false
        values: ["claude", "gpt4", "gemini", "local", "other"]
        description: "Preferred AI model if specified"
        example: "claude"
        producer_source: "Q22 in onboarding-questions.yaml (optional)"
        consumer_usage: "Model-specific guidance"

  # COMMUNICATION SECTION
  communication:
    type: object
    required: true
    description: "Communication style preferences"

    fields:
      language_preference:
        type: enum
        required: true
        values: ["de", "en", "es", "fr", "it", "pt"]
        description: "Alias for personalization.language (backward compatibility)"
        example: "de"
        producer_source: "Copied from personalization.language"
        consumer_usage: "{profile_language_preference} in agents"
        note: "Same as personalization.language, kept for backward compatibility"

      verbosity:
        type: enum
        required: true
        values: ["concise", "balanced", "detailed"]
        description: "Alias for technical.verbosity (backward compatibility)"
        example: "balanced"
        producer_source: "Copied from technical.verbosity"
        consumer_usage: "{profile_communication_verbosity} in agents"
        note: "Same as technical.verbosity, kept for backward compatibility"

      formality:
        type: enum
        required: false
        values: ["casual", "professional", "academic"]
        default: "professional"
        description: "Communication formality level"
        example: "professional"
        producer_source: "Q30 in onboarding-questions.yaml (optional)"
        consumer_usage: "Tone adaptation in responses"

  # WORK CONTEXT SECTION
  work:
    type: object
    required: false
    description: "Work environment context (optional for EA-1.0)"

    fields:
      company:
        type: string
        required: false
        description: "Company name (optional)"
        example: "Acme Corp"
        producer_source: "Q35 in onboarding-questions.yaml (optional)"
        consumer_usage: "Company-specific context (future use)"
        note: "V1.3 will add company-tier profiles"

      team:
        type: string
        required: false
        description: "Team name (optional)"
        example: "Platform Team"
        producer_source: "Q36 in onboarding-questions.yaml (optional)"
        consumer_usage: "Team-specific context (future use)"
        note: "V1.3 will add team-tier profiles"

      domain:
        type: enum
        required: false
        values: ["web", "mobile", "backend", "devops", "data", "ml", "embedded", "other"]
        description: "Primary work domain"
        example: "web"
        producer_source: "Q37 in onboarding-questions.yaml (optional)"
        consumer_usage: "Domain-specific recommendations"

  # CUSTOM INSTRUCTIONS SECTION (EA-1.0.1)
  custom_instructions:
    type: object
    required: false
    description: "User-defined rules and context for AI agents"
    version_added: "v1.0.1"

    fields:
      always_do:
        type: array
        item_type: string
        required: false
        min_items: 0
        max_items: 20
        description: "Rules agents should always follow"
        example: ["Use TypeScript strict mode", "Write tests for all features"]
        producer_source: "Q40 in onboarding-questions.yaml"
        consumer_usage: "{profile_custom_always_do} - agents follow these rules in all responses"
        validation:
          max_items: 20
          item_max_length: 200
        default: []

      never_do:
        type: array
        item_type: string
        required: false
        min_items: 0
        max_items: 20
        description: "Rules agents should never violate"
        example: ["Use var keyword", "Commit secrets to git"]
        producer_source: "Q41 in onboarding-questions.yaml"
        consumer_usage: "{profile_custom_never_do} - agents avoid these actions"
        validation:
          max_items: 20
          item_max_length: 200
        default: []

      domain_knowledge:
        type: string
        multiline: true
        required: false
        max_length: 2000
        description: "Project/team context and domain-specific knowledge"
        example: |
          E-Commerce Platform
          Payment Processing (Stripe, PayPal)
          Microservices architecture
        producer_source: "Q42 in onboarding-questions.yaml (VOLLSTÄNDIG only)"
        consumer_usage: "{profile_custom_domain} - provides context for tailored assistance"
        validation:
          max_length: 2000
        default: ""

  # COMPANY / ENTERPRISE COMPLIANCE SECTION (v1.1, added 2026-04-19)
  # Populated by onboarding v5.0 Q43-Q49 (data_handling_policy required in SMART,
  # others optional and typically VOLLSTÄNDIG-only).
  # All fields are optional — profile remains valid without company.* block.
  company_v11:
    type: object
    required: false
    description: "Enterprise compliance context — populated by onboarding v5.0"
    version_added: "v1.1"
    target_yaml_key: "company"

    fields:
      data_handling_policy:
        type: enum
        required: false
        values: ["local_only", "lan_only", "cloud_llm_allowed", "hybrid"]
        description: "P0 enterprise gate. Locks cloud-LLM vs local-only. Filters all downstream feature availability."
        example: "local_only"
        producer_source: "Q43 in onboarding-questions.yaml (SMART + VOLLSTÄNDIG + MINIMAL)"
        consumer_usage: "{profile_data_handling} - every cloud feature checks this before surfacing"
        enterprise_compliance_priority: "P0"

      approved_llm_providers:
        type: array
        item_type: enum
        required: false
        values: ["ollama_local", "copilot_enterprise", "claude_api", "openai_public", "none"]
        description: "Which LLM providers the organization has approved for use"
        example: ["ollama_local", "copilot_enterprise"]
        producer_source: "Q44 in onboarding-questions.yaml (VOLLSTÄNDIG only)"
        consumer_usage: "Filters LLM integration menu — only approved providers appear"
        enterprise_compliance_priority: "P0"
        default: []

      available_connectors:
        type: array
        item_type: enum
        required: false
        values: ["github_enterprise", "atlassian_cloud", "atlassian_server", "figma", "internal_git", "none"]
        description: "Which tool integrations are approved and reachable"
        example: ["github_enterprise", "atlassian_server"]
        producer_source: "Q45 in onboarding-questions.yaml (VOLLSTÄNDIG only)"
        consumer_usage: "Connector wizard menu filters against this list"
        enterprise_compliance_priority: "P0"
        default: []

      audit_requirements:
        type: enum
        required: false
        values: ["not_required", "local_logs_only", "soc2", "iso27001", "hipaa", "dhl_supply_chain", "other"]
        description: "Audit/compliance framework the user operates under"
        example: "dhl_supply_chain"
        producer_source: "Q46 in onboarding-questions.yaml (VOLLSTÄNDIG only)"
        consumer_usage: "Drives chronicle format + commit signing + log retention"
        enterprise_compliance_priority: "P0"

      network_topology:
        type: enum
        required: false
        values: ["direct_internet", "corporate_proxy", "vpn_required", "air_gapped", "hybrid"]
        description: "Network access pattern for outbound calls"
        example: "vpn_required"
        producer_source: "Q47 in onboarding-questions.yaml (VOLLSTÄNDIG only)"
        consumer_usage: "Every MCP connector + cloud-LLM call respects this"
        enterprise_compliance_priority: "P1"

      compliance_certifications:
        type: array
        item_type: enum
        required: false
        values: ["gdpr", "hipaa", "sox", "iso27001", "pci_dss", "none"]
        description: "Regulations that apply to the work"
        example: ["gdpr"]
        producer_source: "Q48 in onboarding-questions.yaml (VOLLSTÄNDIG only)"
        consumer_usage: "Agent output avoids regulation-violating patterns"
        enterprise_compliance_priority: "P1"
        default: []

      forbidden_git_targets:
        type: array
        item_type: string
        required: false
        max_items: 20
        description: "Git remotes/URLs that must not be pushed to (e.g. tombstoned enterprise targets)"
        example: ["git.dhl.com/azamani1/*", "gitlab.legacy.corp/archive/*"]
        producer_source: "Q49 in onboarding-questions.yaml (VOLLSTÄNDIG only)"
        consumer_usage: "Pre-push guard: warn/block if destination matches"
        enterprise_compliance_priority: "P1"
        default: []
        validation:
          max_items: 20
          item_max_length: 200

  # SAVED CONSENTS SECTION (v1.2, added 2026-04-20)
  # Records which cloud features / connectors the user has explicitly granted
  # consent for. Connector wizards read this BEFORE asking, to avoid re-asking
  # when consent was already given under the same data_handling_context.
  # See .dexCore/_dev/docs/CONSENT-TRACKING.md for the full protocol.
  consents:
    type: array
    required: false
    description: "Explicit user consents for cloud/connector features, tracked to avoid re-asking."
    version_added: "v1.2"
    max_items: 100
    default: []
    item_shape:
      feature_id:
        type: string
        required: true
        description: "Feature ID from features.yaml (e.g. connectors.github_wizard)"
        example: "connectors.github_wizard"
      granted_at:
        type: string
        format: ISO8601
        required: true
        description: "When consent was recorded"
        example: "2026-04-20T10:15:00Z"
      granted_by_command:
        type: string
        required: false
        description: "Which DexHub command surfaced the consent ask (for audit)"
        example: "*github-setup"
      data_handling_context:
        type: enum
        required: true
        values: ["local_only", "lan_only", "cloud_llm_allowed", "hybrid", "unconfigured"]
        description: "company.data_handling_policy AT TIME OF GRANT. Consent expires if policy changes."
        example: "cloud_llm_allowed"
      expires_at:
        type: string
        format: ISO8601
        required: false
        description: "Optional expiry. null = no expiry."
        example: null
      notes:
        type: string
        required: false
        max_length: 500
        description: "Free-form audit notes (why granted, what was explained)"
        example: "Initial setup. User on cloud_llm_allowed policy."

# VALIDATION RULES
validation:
  on_produce:
    - "Validate all required fields are present"
    - "Validate enum values are in allowed list"
    - "Validate array min/max items"
    - "Validate string min/max length"
    - "Fail loudly if validation fails with clear error message"
    - "Log validation errors with field name and expected format"

  on_consume:
    - "Validate schema version compatibility (v1.x)"
    - "Validate all required fields are present"
    - "Validate field types match schema"
    - "Use defaults for missing optional fields"
    - "Fail gracefully with warning if validation fails"
    - "Log warnings for missing optional fields"

# VERSION EVOLUTION
evolution:
  v1.0:
    date: "2025-11-16"
    changes: "Initial schema creation for EA-1.0"
    breaking_changes: []

  v1.0.1:
    date: "2025-11-17"
    changes: "Add custom_instructions section (always_do, never_do, domain_knowledge)"
    breaking_changes: []
    notes:
      - "custom_instructions is optional - backward compatible"
      - "New onboarding questions Q40-Q42"
      - "New workflow variables: {profile_custom_always_do}, {profile_custom_never_do}, {profile_custom_domain}"

  v1.1:
    date: "2026-04-19"
    changes: "Add company.* block for enterprise compliance (Q43-Q49 from onboarding v5.0)"
    breaking_changes: []
    notes:
      - "7 new fields: data_handling_policy, approved_llm_providers, available_connectors, audit_requirements, network_topology, compliance_certifications, forbidden_git_targets"
      - "All optional — v1.0 profiles remain valid without company.* block"
      - "data_handling_policy is P0 enterprise gate (in SMART v5)"
      - "Other 6 are P0/P1 compliance context (VOLLSTÄNDIG v5 only)"

  v1.2:
    date: "2026-04-20"
    changes: "Add `consents` array for saved-consent tracking (Phase 5.1.c Tier 3.1)"
    breaking_changes: []
    notes:
      - "New field: consents (array of consent entries)"
      - "Each entry: feature_id, granted_at, granted_by_command, data_handling_context, expires_at, notes"
      - "Purpose: avoid re-asking users who have already consented under the same data_handling_policy"
      - "Protocol: see .dexCore/_dev/docs/CONSENT-TRACKING.md"
      - "v1.0/v1.1 profiles remain valid — consents defaults to []"

  v2.0_planned:
    date: "TBD"
    changes: "Guardian governance integration"
    breaking_changes: ["Schema structure may change for guardian tier"]
    note: "V2.0 feature - enterprise governance"

# MIGRATION GUIDE
migration:
  from_no_profile:
    description: "First-time profile creation"
    steps:
      - "Run myDex onboarding (SMART or VOLLSTÄNDIG)"
      - "Profile generated automatically"
      - "All agents load profile on next run"

  from_v1.0_to_v1.1:
    description: "Future migration when v1.1 is released"
    steps: "TBD when v1.1 is defined"

# EXAMPLES
examples:
  minimal_valid_profile:
    description: "Minimum required fields for valid profile"
    yaml: |
      version: "1.0"
      created_at: "2025-11-16T12:00:00Z"
      personalization:
        name: "Alex"
        language: "en"
        role: "developer"
      technical:
        experience_level: "intermediate"
        code_style: "standard"
        verbosity: "balanced"
      tech:
        primary_stack: ["javascript"]
      ai:
        readiness: "intermediate"
        copilot_enabled: false
      communication:
        language_preference: "en"
        verbosity: "balanced"
      # custom_instructions is optional - omitted in minimal profile

  complete_profile:
    description: "Fully populated profile with all optional fields"
    yaml: |
      version: "1.0"
      created_at: "2025-11-16T12:00:00Z"
      last_updated: "2025-11-16T14:00:00Z"
      personalization:
        name: "Alex"
        language: "de"
        role: "developer"
      technical:
        experience_level: "senior"
        code_style: "airbnb"
        verbosity: "concise"
      tech:
        primary_stack: ["typescript", "react", "nodejs"]
        secondary_stack: ["python", "docker"]
        interests: ["rust", "kubernetes"]
      ai:
        readiness: "advanced"
        copilot_enabled: true
        preferred_model: "claude"
      communication:
        language_preference: "de"
        verbosity: "concise"
        formality: "professional"
      work:
        company: "Acme Corp"
        team: "Platform Team"
        domain: "web"
      custom_instructions:
        always_do:
          - "Use TypeScript strict mode"
          - "Write tests for all features"
          - "Follow Airbnb style guide"
        never_do:
          - "Use var keyword"
          - "Commit secrets to git"
          - "Skip code reviews"
        domain_knowledge: |
          E-Commerce Platform for Acme Corp
          Payment processing with Stripe and PayPal
          Microservices architecture with Node.js and React
          Kubernetes deployment on AWS EKS

# RELATED FILES
related_files:
  producer:
    - ".dexCore/_cfg/onboarding-questions.yaml"
    - ".dexCore/core/agents/mydex-agent.md"
    - "myDex/.dex/config/profile.yaml.example"

  consumer:
    - ".dexCore/core/tasks/workflow.xml"
    - "All 39 agents in .dexCore/core/agents/, .dexCore/dxm/agents/, .dexCore/dis/agents/"

  validation:
    - ".dexCore/_dev/tools/validate_profile_schema.py"

  documentation:
    - ".dexCore/_dev/docs/PROFILE-SCHEMA-GUIDE.md"
    - ".dexCore/_dev/DEVELOPMENT-QUALITY-STANDARDS.md"
    - ".dexCore/_dev/learnings/PROFILE-SCHEMA-MISMATCH-2025-11-16.md"

# NOTES
notes:
  - "This schema is the SINGLE SOURCE OF TRUTH for profile structure"
  - "Producer MUST validate before saving, Consumer MUST validate on load"
  - "Integration test validates end-to-end flow"
  - "communication.language_preference and communication.verbosity are aliases for backward compatibility"
  - "work.company and work.team are placeholders for V1.3 multi-tier profiles"
  - "Schema version MUST be checked on both producer and consumer side"
